# Relative paths work out of the box when commands are run from this repository.
MODEL_ROOT=local/models
DATA_ROOT=local/data/SuperNI
ARTIFACT_ROOT=local/artifacts
OUTPUT_ROOT=outputs
NPROC_PER_NODE=8

