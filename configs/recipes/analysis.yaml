experiment_id: ANALYSIS
split_manifest: ${ARTIFACT_ROOT}/splits/fidelity.json
profile_source_dir: ${ARTIFACT_ROOT}/profiles
tf_assignment_path: ${ARTIFACT_ROOT}/assignments/tf.json
initial_bank_checkpoint: ${ARTIFACT_ROOT}/checkpoints/initial_bank.pt
output_dir: ${OUTPUT_ROOT}/analysis
fidelity_probes_per_task: 8
fidelity_eta: 0.01
world_size: 8

